<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.oa.mapper.LeaveApplicationMapper">

    <!--
        新增请假申请单
        业务逻辑：写入请假单基础信息，状态默认为待审批
    -->
    <insert id="insertLeaveApplication" parameterType="com.buu.oa.entity.LeaveApplication" useGeneratedKeys="true" keyProperty="entity.id">
        INSERT INTO leave_application (leave_no, emp_id, leave_type, start_date, end_date, days, reason, status)
        VALUES (#{entity.leaveNo}, #{entity.empId}, #{entity.leaveType}, #{entity.startDate}, #{entity.endDate}, #{entity.days}, #{entity.reason}, #{entity.status})
    </insert>

</mapper>